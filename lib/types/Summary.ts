export type Summary = {
    release_year: String,
    total_movies: number,
    total_series: number,
    watched_movies: number,
    watched_series: number,
    movie_avg_rating: number,
    series_avg_rating: number
}